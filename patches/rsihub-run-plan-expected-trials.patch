diff --git a/scaffolds/evaluators/harbor/parse_score.py b/scaffolds/evaluators/harbor/parse_score.py
index 3646840..4c8c225 100644
--- a/scaffolds/evaluators/harbor/parse_score.py
+++ b/scaffolds/evaluators/harbor/parse_score.py
@@ -34,0 +35,8 @@ def _expected_trials(run_dir: Path, env_values: dict[str, str]) -> int:
+    run_plan = run_dir / "run-plan.json"
+    if run_plan.exists():
+        payload = json.loads(run_plan.read_text())
+        expected = payload.get("expected_trials") if isinstance(payload, dict) else None
+        if isinstance(expected, int) and not isinstance(expected, bool) and expected > 0:
+            return expected
+        raise ValueError("run-plan.json has invalid expected_trials")
+
diff --git a/src/evolve/evaluation/execution.py b/src/evolve/evaluation/execution.py
index cd68f97..68d3f87 100644
--- a/src/evolve/evaluation/execution.py
+++ b/src/evolve/evaluation/execution.py
@@ -377 +377 @@ def _runtime_selection_matches(
-    source = selection if selection.is_file() else plan
+    source = plan if plan.is_file() else selection
